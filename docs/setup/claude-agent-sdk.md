# Create Your Own Claude Agent with Larry

Status: coming soon.

This guide will show how to build a custom Claude-based agent around Larry generators using the Agent SDK.

## Planned content

- Required SDK setup
- Registering Larry as a tool or generator backend
- Building an agent loop around schema-based generation
- Passing project context safely
- Validating outputs and handling retries
- Example architecture for a reusable internal agent

## Outcome

The final version of this guide will provide an example implementation and explain when a custom agent is a better fit than editor-native integrations.
